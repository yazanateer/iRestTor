/**
 * TS mirror of the color values in resources/css/tokens.css, for the rare
 * spots (native <input type="color"> defaults) that need a literal hex
 * string rather than a CSS var() reference. Keep in sync with tokens.css.
 */
export const BRAND_BLUE = '#2563ff';
export const BRAND_BLUE_SOFT = '#3b82f6';
export const COLOR_SUCCESS = '#16a34a';
